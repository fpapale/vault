# Wiki Log

Record cronologico append-only di tutte le operazioni (ingest, query, lint).

## [2026-04-22] init | Inizializzazione Infrastruttura
Inizializzata la struttura base del wiki, comprendente le cartelle `raw`, `wiki` e i file `index.md`, `log.md` e `schema.md`.
